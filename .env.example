# Ferelix Server - Environment Configuration
# All variables are optional with sane defaults.
# Copy this file to .env and customize as needed.

# ================================
# SECURITY
# ================================

# JWT Secret Key - Auto-generated in Docker, or uses dev default for local development
# For production outside Docker: generate with `openssl rand -hex 32`
# SECRET_KEY=your-generated-secret-key-here

# ================================
# DATABASE
# ================================

# SQLite (default)
# DATABASE_URL=sqlite+aiosqlite:///./ferelix.db

# PostgreSQL (if you're managing your own PostgreSQL server)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/ferelix

# ================================
# JWT TOKEN CONFIGURATION
# ================================

# Access token lifetime (in minutes)
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# Refresh token lifetime (in days)
# REFRESH_TOKEN_EXPIRE_DAYS=7

# ================================
# CORS CONFIGURATION
# ================================

# Allowed origins for CORS (comma-separated)
# Use "*" for development, restrict in production
# ALLOWED_ORIGINS=*

# Production example:
# ALLOWED_ORIGINS=https://ferelix.yourdomain.com,https://app.yourdomain.com

# ================================
# SERVER CONFIGURATION
# ================================

# Server host (0.0.0.0 for Docker, 127.0.0.1 for local dev)
HOST=0.0.0.0

# Server port
PORT=8000

# ================================
# DOCKER-SPECIFIC (if using PostgreSQL)
# ================================

# PostgreSQL password (used in docker-compose.yml)
# DB_PASSWORD=secure-database-password
